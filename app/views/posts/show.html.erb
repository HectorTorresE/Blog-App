<div class="border rounded-md p-2 m-4">
  <h1 class=" font-semibold
        text-sky-600
        hover:text-sky-700"><%= @post.title %> by <%= @post.author.name %></h1>
 
  <p class="text-right text-sm font-bold ">Comments:
    <%= @post.commentscounter || 0 %>
    Likes:
    <%= @post.likescounter || 0 %></p>
  <p class="my-4"><%= @post.text %></p>
  <hr>
  <h4 class="font-semibold mt-4">Comments:</h4>
    <ul class="my-3">
      <% @post.comments.each do |comment| %>
        <li class="text-sm my-2">
          <span class="font-semibold"><%= comment.author.name %>: <%= comment.text %>
        </li>
      <% end %>
    </ul>
</div>
<div class="flex flex-row justify-evenly">
  <p class="text-center my-2"><%= link_to 'Add Comment', new_user_post_comment_path(@post.author, @post), class: "border px-2 py-1 bg-sky-500 hover:bg-sky-600 font-bold rounded-md" %></p>
  <p class="text-center my-2"><%= button_to 'Like', user_post_likes_url(@post.author, @post), class: "border px-2 py-1 bg-red-500 hover:bg-red-600 font-bold rounded-md" %></p>
</div>